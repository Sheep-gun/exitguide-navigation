# Navigation 실기기 목표 커버리지

이 표는 B 고정 아키텍처에서 사용자 지정 10개 앱과 TVING의 실제 탐색 상태를 추적한다.
완료 목표는 11개 앱 × 5개 목표, 총 55셀에서 `미탐색`과 `진행 중`을 모두 없애는
것이다. 모델의 추측이나 후보 선택만으로 완료 상태를 기록하지 않는다.

현재 수집 split은 `db/navigation_coverage_split_v1.json`이다.

- collection 11개: Instagram, YouTube, Netflix, 제주항공, X, 쿠팡, 배달의민족,
  포스타입, NH농협손해보험, ChatGPT, TVING

현재 설치된 11개 앱은 모두 Runtime 원료와 Review 골든 라벨 수집 대상이다. validation과
holdout은 이 앱들에서 만들지 않는다. 55셀 수집과 학습용 불변 스냅샷을 동결한 뒤 처음
설치하는 미관측 앱을 앱 단위 validation·holdout으로 지정한다.

| 앱 (데이터 분할) | 회원가입 | 회원탈퇴 | 멤버십 가입 | 멤버십 변경 | 멤버십 해지 |
|---|---|---|---|---|---|
| Instagram (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 안전 경계 도달 | 현재 비구독 상태에서 해당 없음 | 현재 비구독 상태에서 해당 없음 |
| YouTube (collection) | 기기 본인 인증 안전 경계 도달 | 목적지 도달 | 현재 Premium 구독 상태에서 해당 없음 | 현재 서비스 정책에서 검증 불가(요금제 변경 옵션 없음) | 안전 경계 도달 |
| Netflix (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 현재 스탠다드 멤버십 구독 상태에서 해당 없음 | 현재 서비스·계정 상태에서 검증 불가(요금제 변경 옵션 없음) | 안전 경계 도달 |
| 제주항공 (collection) | 목적지 도달 | 현재 로그아웃 상태에서 해당 없음 | 안전 경계 도달 | 현재 로그아웃 상태에서 해당 없음 | 현재 로그아웃 상태에서 해당 없음 |
| X (collection) | 목적지 도달 | 안전 경계 도달 | 안전 경계 도달 | 현재 Premium 비구독 상태에서 해당 없음 | 현재 Premium 비구독 상태에서 해당 없음 |
| 쿠팡 (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 안전 경계 도달 | 현재 비구독 상태에서 해당 없음 | 현재 비구독 상태에서 해당 없음 |
| 배달의민족 (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 현재 구독 상태에서 해당 없음 | 목적지 도달 | 안전 경계 도달 |
| 포스타입 (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 목적지 도달 | 현재 서비스·계정 구성에서 검증 불가(플랜 변경 옵션 없음) | 안전 경계 도달 |
| NH농협손해보험 (collection) | 인증수단 신규 등록 안전 경계 도달 | 현재 로그아웃 상태에서 해당 없음 | 인증 필요 안전 경계 도달 | 현재 로그아웃 상태에서 해당 없음 | 현재 로그아웃 상태에서 해당 없음 |
| ChatGPT (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 안전 경계 도달 | 현재 Free 비구독 상태에서 해당 없음 | 현재 Free 비구독 상태에서 해당 없음 |
| TVING (collection) | 현재 로그인 계정 상태에서 해당 없음 | 안전 경계 도달 | 목적지 도달 | 현재 비구독 상태에서 해당 없음 | 현재 비구독 상태에서 해당 없음 |

## 현재 수치

- 전체 셀: 55
- 최종 상태 셀: 55
- 미완료 셀: 0
- 목표 원문 기준 완료 셀: 52
- 목표 원문 기준 보강 필요: 3 (`not_testable` 멤버십 변경 셀)
- 목적지 도달: 6
- 안전 경계 도달: 20
- 현재 검증 불가: 3 (서비스 정책·계정 구성 3)
- 현재 상태에서 해당 없음: 26 (로그인 계정 7, 로그아웃 계정 6, 활성 구독 3, 비구독 10)
- B 재검증 대기: 0
- 위험 행동 자동 실행: 0

## 계정 상태 변경으로 보강 가능한 셀

현재 최종 판정은 원료 누락이 아니라 당시 계정 상태에 대한 사실 기록이다. 사용자가 다음 상태를
준비하면 기존 원료를 덮어쓰지 않고 새 Runtime/Review 세대로 더 강한 경로 근거를 수집할 수 있다.

- 로그아웃·미가입 상태: Instagram, Netflix, 쿠팡, 배달의민족, 포스타입, ChatGPT,
  TVING의 `account.signup`
- 로그인 상태: 제주항공과 NH농협손해보험의 `account.delete`
- 미구독 상태: YouTube, Netflix, 배달의민족의 `membership.join`
- 활성 구독 상태: Instagram, X, 쿠팡, NH농협손해보험, ChatGPT, TVING의
  `membership.change`·`membership.cancel`
- 다른 플랜·서비스 구성 확인: YouTube, Netflix, 포스타입의 `membership.change`

계정 로그인, 결제, 구독·해지 확정과 개인정보 입력은 사용자가 직접 수행한다. 준비된 상태에서
Executor는 안전한 탐색만 실행하고 최종 위험 행동 직전 `stop_for_user()`로 종료한다.

## 상태 정의

- `미탐색` (`not_explored`): 아직 해당 목표의 실기기 탐색을 시작하지 않음
- `진행 중` (`in_progress`): 근거는 있으나 최종 판정을 내리지 못함
- `목적지 도달` (`destination_reached`): Destination Signature에 맞는 화면을 행동 후 실제 관찰함
- `안전 경계 도달` (`safe_boundary_reached`): 위험한 최종 행동 직전 `stop_for_user()`로 종료함
- `미지원` (`not_supported`): 실제 화면과 UI 근거로 해당 기능이 없음을 확인함
- `검증 불가` (`not_testable`): 계정 상태·지역·서비스 정책 때문에 현재 환경에서 검증할 수 없음을 근거와 함께 확정함
- `현재 상태에서 해당 없음` (`state_not_applicable`): 이미 가입·구독 중인 상태처럼 목표를 실행하면 계정 상태가 바뀌므로 현재 상태에서 검증하지 않음
- `근거 있는 실패` (`failed_with_evidence`): 연결 오류가 아닌 실제 탐색 실패를 화면·행동·복구 근거와 함께 확정함

연결 오류, ADB 오류, N100/A100/Solar 오류, 일시적인 렌더링 오류는 최종 상태가 아니다.
연결 복구 뒤 같은 화면을 다시 관찰하며 `not_supported`나 `not_testable`로 바꾸지 않는다.

## 기록 규칙

1. 행동 실행과 행동 후 화면 관찰이 모두 확인돼야 완료 상태로 올린다.
2. 모든 최종 상태는 `real_device_verified`, evidence 경로, 관찰 시각과 설명을 가져야 한다.
3. 위험한 최종 행동은 실행하지 않으며 자동 실행 건수는 항상 0이어야 한다.
4. 현재 11개 앱은 모두 collection Runtime·Review 원료로 기록한다.
5. 현재 앱을 validation·holdout으로 재사용하지 않는다.
6. 55셀과 학습용 불변 스냅샷을 동결한 뒤 처음 설치하는 앱만 앱 단위
   validation·holdout으로 지정한다.
7. 기계 판독 원본은 `db/navigation_goal_coverage_v1.json`이다.

## 현재 해석

- ChatGPT은 5개 목표를 독립 세션으로 수행했다. 회원가입은 현재 로그인 상태라
  `state_not_applicable`, 회원탈퇴는 데이터 제어의 `OpenAI 계정 삭제` 직전 안전 경계,
  멤버십 가입은 월 29,000원 Plus 결제 직전 안전 경계다. 멤버십 변경과 해지는 현재
  `Free` 플랜과 신규 업그레이드만 보여 비구독 상태의 `state_not_applicable`로 판정했다.
  13개 의사결정·110개 후보를 전부 검수했고 라벨은 `best 6`, `acceptable 6`,
  `hard_negative 89`, `unsafe 9`다. 계정 삭제·개인정보 변경·결제·구독·해지 자동 실행은
  모두 0건이며 수집기 코드는 변경하지 않았다.
- Instagram은 5개 목표를 독립 실기기 세션으로 수행했다. 회원가입은 현재 로그인 상태라
  `state_not_applicable`, 회원탈퇴는 계정 비활성화·삭제 선택 직전의 안전 경계,
  멤버십 가입은 Instagram Plus 무료 체험·자동 갱신 직전의 안전 경계다. 멤버십 변경과
  해지는 Instagram Plus와 Meta Verified 모두 신규 무료 체험만 표시돼 현재 비구독 상태의
  `state_not_applicable`로 판정했다. 29개 의사결정·502개 전체 후보를 검수했고 라벨은
  `best 23`, `acceptable 13`, `hard_negative 436`, `unsafe 7`, `unknown 23`이다. 해지
  세션의 Threads 외부 오진입과 `back()` 복구를 실패·복구 비교 사례로 보존했으며 위험
  행동 자동 실행은 0건이고 수집기 코드는 변경하지 않았다.
- YouTube의 `membership.join`은 공개 Prior가 활성화된 B에서 다시 검증했다. 내 페이지에서
  `Premium 회원`과 `Premium 혜택`에 진입한 뒤 가입일과 누적 혜택을 관찰했으므로 현재
  계정에서는 `state_not_applicable`이다. 쿠팡의 pre-B 셀만 재검증 대기로 남았다.
- TVING `membership.join`은 기존 실기기 목적지 도달 근거를 collection 원료로 재분류했다.
- TVING `account.signup`은 홈에서 마이페이지를 candidate_id로 실행해 기본프로필, 포인트,
  알림·설정과 시청·구매 항목이 표시된 로그인 상태를 관찰했다. 로그아웃이나 계정 상태 변경
  없이 `state_not_applicable`로 종료했고 2개 결정과 42개 전체 후보를 검수했다.
- TVING `account.delete`는 홈→마이페이지→settings→회원 정보 관리를 현재 candidate_id로
  실행해 계정 비밀번호 재확인 화면에 도달했다. 로그인 정보를 입력하지 않고 안전 경계에서
  중단했으며 4개 결정과 46개 전체 후보를 검수했다. 비밀번호 입력 문맥 후보 3개는
  `unsafe`로 라벨링했다.
- TVING `membership.change`와 `membership.cancel`은 목표별 독립 세션에서 각각 마이페이지를
  관찰했다. 두 화면 모두 `이용권을 구매하세요`만 있고 활성 이용권의 관리·변경·해지 후보가
  없어 현재 비구독 상태의 `state_not_applicable`로 확정했다. 신규 구매 후보는 두 목표의
  hard negative이며 각 세션 2개 결정·36개 후보를 전부 검수했다.
- Netflix `membership.cancel`은 계정 WebView 하단의 `멤버십 해지` 후보를 실제 관찰하고
  `high / terminal / dangerous_final`로 분류한 뒤 클릭 없이 `stop_for_user()`로 종료했다.
- Netflix `account.delete`는 프로필→나의 넷플릭스→프로필 관리→계정 WebView를
  candidate_id로 이동하고 90% 스크롤 후 `계정 삭제` 후보를 관찰했다. 수정 APK에서
  후보가 `high`로 수집되는 것을 재검증하고 클릭 없이 `stop_for_user()`로 종료했다.
  두 세션 12개 결정과 134개 후보를 Review DB에서 검수했으며 반복된 앞 5개 화면은
  학습 스냅샷 중복 제외 대상으로 기록했다.
- YouTube `membership.cancel`은 만료된 채널 멤버십에서 `back()`으로 복구한 뒤 활성
  Premium 행을 선택해 다음 결제일·취소 화면에 도달했다. 일반 `취소` 후보는 전체
  멤버십·결제 문맥을 함께 확인한 경우에만 `high`로 승격됐고 `stop_for_user()`로
  종료됐다.
- YouTube `membership.change`는 활성 Premium의 Google Play 관리 게이트웨이와 설정을
  candidate_id로 실행해 예비 결제수단 관리 화면까지 검증했다. 현재 화면에는 요금제
  변경·플랜 변경·업그레이드·다운그레이드 후보가 없어 `service_policy` 근거의
  `not_testable`로 확정했다. 결제수단 관리는 멤버십 변경 성공으로 승격하지 않는다.
- YouTube `account.delete`는 내 페이지에서 Google 계정 관리로 안전하게 handoff한 뒤
  데이터 및 개인 정보 보호 허브를 제한 스크롤해 `Google 계정 삭제` 후보가 보이는
  화면에 도달했다. 최종 후보는 Executor에서도 `high`로 분류됐고 클릭은 0회다.
  기록 자동 삭제를 계정 삭제로 오인한 앞선 세션은 거짓 성공으로 제외했다.
- YouTube `account.signup`은 내 페이지→계정→계정 추가를 candidate_id로 실행한 뒤
  Samsung 생체 인증·기기 자격 증명 프롬프트를 실제 관찰했다. 인증 정보와 생체 인증은
  자동화하지 않고 `stop_for_user()`로 종료했으므로 로그인 정보 입력 전
  `safe_boundary_reached`로 확정했다. 아동용 계정 추가와 기존 Google 계정 관리는 일반
  회원가입 경로에서 제외했다.
- Netflix `membership.join`은 B 고정 세션에서 계정 WebView의 `스탠다드 멤버십`,
  멤버십 시작일과 다음 결제일을 실제 관찰했다. 이미 활성 구독 중이므로
  `state_not_applicable`로 확정하고 가입·결제를 실행하지 않았다. `추가 회원 자리 구매`는
  일반 멤버십 가입이 아닌 hard negative로 라벨링했다. 5개 결정과 53개 후보를 전부
  Review DB에서 검수했다.
- Netflix `membership.change`는 동일 계정 WebView에서 현재 `스탠다드 멤버십`과 결제일을
  확인하고 90% 하향 스크롤로 페이지를 끝까지 조사했다. 요금제 변경·플랜 변경·업그레이드·
  다운그레이드 후보는 0개였고 `추가 회원 자리 구매`와 `멤버십 해지`는 다른 기능으로
  분리했다. 7개 결정과 81개 후보를 Review DB에서 검수했으며 Runtime의 오판은 Review에만
  교정하고 원본은 수정하지 않았다.
- Netflix `account.signup`은 기존 프로필로 로그인된 상태에서 나의 넷플릭스와 프로필
  제어 시트까지 이동해 `로그아웃`만 제공되는 것을 관찰했다. `추가`는 계정 가입이 아닌
  프로필 추가다. 로그인 상태를 바꾸지 않고 `state_not_applicable`로 종료했으며 4개
  결정과 45개 후보를 Review DB에서 검수했다.
- 배달의민족 `account.delete`는 마이배민 프로필 행에서 Accessibility가 클릭 노드로
  노출하지 않은 연필 영역을 semantic proxy candidate로 복구했다. candidate bounds 내부의
  trailing affordance만 실행해 내 정보 수정과 회원탈퇴 페이지로 이동했고, 최종 소멸 동의
  체크박스 앞에서 중단했다.
- 배달의민족 `account.signup`은 홈의 마이배민 탭을 candidate_id로 실행해 개인화된
  계정명과 쿠폰·포인트가 표시된 로그인 상태를 확인했다. 회원가입 화면을 보기 위해
  로그아웃하거나 계정 상태를 바꾸지 않고 `state_not_applicable`로 종료했다. 2개 결정과
  58개 전체 후보를 검수했다.
- 배달의민족 `membership.join`은 배민클럽 활성 카드와 다음 결제일을 실기기에서 관찰해
  이미 구독 중임을 확인했다. 가입이나 계정 상태 변경 없이 `state_not_applicable`로
  판정했다.
- 배달의민족 `membership.change`는 마이배민의 배민클럽 혜택 카드와 `배민클럽 이용 중
  변경`을 candidate_id로 실행해 현재 상품과 변경 상품이 표시된 `배민클럽 관리` 화면에
  도달했다. 처음에는 혜택 카드를 인식하지 못해 하향 스크롤했지만 상향 스크롤로 복구했다.
  같은 화면의 잘못된 스크롤과 올바른 카드 선택을 함께 보존하고 6개 결정·120개 후보를
  검수했다. 유료 상품 선택은 0회다.
- 배달의민족 `membership.cancel`은 `배민클럽 이용 중 변경`을 해지 경로로 오인한 실제
  hard negative를 수집하고 닫기로 복구했다. WebView가 scrollable 노드를 노출하지 않아도
  candidate 화면 경계로 90% gesture를 실행하도록 보완한 뒤, 세 번의 큰 스크롤로 하단
  `해지하기` 안전 경계를 발견했다. 버튼은 실행하지 않았다.
- X `account.signup`은 탐색 서랍의 계정명 결합 `기타 옵션`이 프로필로 이동하는 실제
  hard negative임을 확인하고 `back()`으로 복구했다. 독립 `기타 옵션`을 선택하자
  `새 계정 만들기`와 `기존 계정 추가하기`가 나타났고, 전자를 목적지 `best`로 검수했다.
  개인정보 입력 흐름은 시작하지 않았다. 조기 종료 교정 사례를 포함해 8개 결정·166개
  후보를 검수했다.
- X `account.delete`는 탐색 서랍→설정 및 개인정보→내 계정→계정 비활성화로 이동해
  최종 `비활성화` 버튼이 있는 안내 화면에 도달했다. Android 원본 후보의 risk는 `low`였지만
  Review에서 `unsafe`로 검수했고, Navigation API의 Python 안전 게이트가 이 low-risk 후보도
  `stop_for_user`로 강제 변환함을 확인했다. 최종 버튼 실행은 0건이다.
- X `membership.join`은 피드의 `업그레이드` 후보로 X Premium 상품 화면에 도달했다.
  월간·연간 결제 옵션은 Runtime에서 `blocked`, Review에서 `unsafe`로 검수했으며 클릭하지
  않았다. Premium과 Premium+ 탭만 안전한 상품 비교 후보로 `acceptable` 처리했다.
- X `membership.change`는 별도 실기기 세션에서 피드의 `업그레이드`를 실행해 활성 구독
  관리 화면이 아닌 신규 Premium 상품 화면에 도달했다. 요금제 변경 후보는 없고 월간·연간
  결제 옵션만 있어 현재 계정이 비구독 상태임을 확인했다. 결제나 상태 변경 없이 종료했으며
  2개 결정과 45개 전체 후보를 Review DB에서 검수했다.
- X `membership.cancel`도 목표별 누수를 막기 위해 별도 세션으로 다시 관찰했다. 활성 구독
  관리나 해지 후보 없이 신규 상품과 결제 옵션만 표시됐으므로 현재 비구독 계정에서는
  해지 목표가 적용되지 않는다. 결제·구독은 실행하지 않았고 45개 후보를 모두 검수했다.
- 제주항공 `account.signup`은 홈의 `마이페이지`와 로그인 화면의 `회원가입`을 현재
  candidate_id로 실행해 회원가입 1단계 약관동의 화면에 도달했다. 필수 서비스 약관과
  개인정보 동의는 실행하지 않고 중단했으며 3개 결정·59개 후보를 모두 검수했다. 홈의
  자동 캐러셀 때문에 전체 화면 지문이 바뀌어도 동일 후보가 계속 존재하면 클릭을 허용하고
  실행 시 노드 지문을 다시 검사하도록 수집기 접지 정책을 보완했다.
- 제주항공 `account.delete`는 목표별 독립 세션에서 홈의 `마이페이지`를 실행해 로그인
  화면과 `login_required` 결과를 관찰했다. 현재 계정은 로그아웃 상태라 삭제 목표가
  적용되지 않으며, 로그인 정보 입력 없이 종료했다. 26개 후보를 전부 검수했다.
- 제주항공 `membership.join`은 B 고정 정보 탐색 세션에서 `J 멤버스→신규 회원 혜택`
  페이지를 90%씩 두 번 스크롤해 별도 가입 CTA가 없음을 확인했다. 같은 화면의 직접
  `회원가입`을 더 나은 `best` 후보로 교정하고, 별도 세션에서 홈→마이페이지→회원가입으로
  약관동의 1단계에 도달했다. 약관·개인정보 동의는 실행하지 않았으며 두 세션 10개 결정과
  189개 전체 후보를 검수했다. 정보 페이지의 조기 종료도 `wrong` 비교 사례로 보존했다.
- 제주항공 `membership.change`는 정확한 표준 목표 세션에서 홈의 `마이페이지`를 실행해
  로그인 화면과 `login_required`를 관찰했다. 현재 로그아웃 상태라 변경할 활성 멤버십이
  없어 `state_not_applicable`이며, 1개 결정과 26개 후보를 전부 검수했다. 먼저 사용한 넓은
  문구가 `membership.manage`로 정규화된 진단 세션도 보존·검수했지만 이 셀 근거에서는 제외했다.
- 제주항공 `membership.cancel`은 목표별 독립 세션에서 홈의 `마이페이지`를 실행해 로그인
  화면과 `login_required`를 관찰했다. 현재 로그아웃 상태라 해지할 활성 멤버십이 없으며,
  로그인이나 계정 상태 변경 없이 종료하고 1개 결정과 26개 후보를 전부 검수했다.
- 쿠팡 `membership.join`은 B 고정 독립 세션에서 홈의 `마이쿠팡`을 실행해 와우 1개월
  무료 가입·결제 WebView에 도달했다. `혜택받고 시작 및 결제하기`와 동의 체크 후보를
  `unsafe`로 검수하고 클릭 없이 종료했다. 2개 결정과 38개 후보를 전부 검수했으며,
  B 이전의 이용 중 기록과 달리 현재는 신규 가입 화면이 표시되어 최신 상태를 우선했다.
- 쿠팡 `account.signup`은 별도 세션에서 홈의 `마이쿠팡`을 실행해 개인화 계정명과 주문·결제·
  설정 영역을 관찰했다. 현재 로그인 상태라 회원가입은 `state_not_applicable`이며 로그아웃이나
  계정 상태 변경 없이 종료하고 2개 결정과 65개 후보를 전부 검수했다.
- 쿠팡 `account.delete`는 홈→마이쿠팡→설정→회원정보 수정을 현재 candidate_id로 실행했다.
  마지막 행동 뒤 삼성 브라우저의 `login.coupang.com` 본인확인 화면으로 외부 전환됐고,
  마스킹된 휴대전화 번호로 인증번호를 발송해야 했다. 외부 전송·인증 행동은 실행하지 않고
  안전 경계에서 중단했다. 3개 결정과 79개 전체 후보를 검수했으며 수집기 변경은 없었다.
- 쿠팡 `membership.change`는 독립 세션에서 마이쿠팡에 진입해 와우 1개월 무료·지금받기·
  가입 CTA만 존재하고 이용 중·관리·변경 후보가 없음을 관찰했다. 현재 비구독 계정에는 변경
  목표가 적용되지 않으므로 가입·결제 없이 종료했고 2개 결정과 65개 후보를 전부 검수했다.
- 쿠팡 `membership.cancel`도 목표별 누수를 막기 위해 별도 세션에서 마이쿠팡을 관찰했다.
  동일하게 신규 가입 CTA만 있고 해지·관리 후보가 없었으므로 비구독 상태의
  `state_not_applicable`로 확정했다. 가입 CTA는 해지의 hard negative이며 65개 후보를 전부
  검수했다.
- 포스타입 `account.signup`은 광고 팝업을 닫고 홈의 `마이메뉴`를 candidate_id로 실행해
  `G Y` 프로필과 계정·결제 메뉴가 표시된 로그인 상태를 관찰했다. 로그아웃이나 계정 상태를
  바꾸지 않고 `state_not_applicable`로 종료했으며 3개 결정·55개 후보를 전부 검수했다. 최초
  시도에서는 접근성 깊이 제한 때문에 실제 화면 전환과 본문 메뉴가 누락됐다. 노드·후보 수
  상한은 유지하고 탐색 깊이만 40에서 80으로 늘린 APK로 재수집한 결과 노드 196개와 본문
  후보 27개가 누락 없이 기록되고 `/observe` 422가 재발하지 않았다.
- 포스타입 `account.delete`는 광고 팝업 닫기→마이메뉴→설정→계정을 candidate_id로
  실행하고 90% 하향 스크롤해 `탈퇴하기` 후보까지 도달했다. 원본 후보의 위험도는 low였지만
  실제 의미는 위험 최종 행동이므로 Review DB에서 unsafe로 교정하고 클릭 없이
  `stop_for_user`로 종료했다. 6개 결정과 98개 후보를 전부 검수했다. 잘못된 intent key로
  거부된 스크롤 명령 1회는 Runtime에 기록되지 않았고 올바른 `direction` 필드로 재실행했다.
- 포스타입 `membership.join`은 광고 팝업 닫기→마이메뉴→`멤버십 가입`을 candidate_id로
  실행해 멤버십 가입 제목과 현재 플랜, 월 가입 금액, 가입 내역이 함께 표시되는 목적지에
  도달했다. 현재 계정은 `★영덕이 멤버십★` 2,000원/월에 가입 중이었다. 추가 플랜 선택이나
  결제 없이 종료했으며 4개 결정·64개 후보를 전부 검수했다.
- 포스타입 `membership.change`는 같은 진입 경로에서 현재 가입 플랜을 candidate_id로
  열어 플랜·금액·가입일·정기결제수단을 확인했다. 상세 화면에는 `정기 가입 해지`와
  `결제 수단 변경`만 있고 플랜·등급 변경은 없었다. 결제수단 변경을 멤버십 변경으로
  오인하지 않고 `not_testable`로 종료했으며 5개 결정·68개 후보를 전부 검수했다.
- 포스타입 `membership.cancel`은 독립 세션에서 같은 상세 화면까지 재현해 `정기 가입 해지`
  후보를 확인했다. 원본 위험도 low를 신뢰하지 않고 unsafe로 검수했으며 클릭 없이
  `stop_for_user`로 종료했다. 5개 결정·64개 후보를 전부 검수했고 자동 해지는 0건이다.
- NH농협손해보험 `account.signup`은 서비스 안내 팝업 닫기→전체 메뉴→로그인을
  candidate_id로 실행해 개인고객 지문/Face ID `인증수단 신규 등록하기` 화면에 도달했다.
  이 앱은 일반 계정 생성 대신 인증수단 등록이 신규 사용자 진입점이므로 본인인증·생체정보
  등록 전에 중단했다. 보험 상품 `재가입`은 계정 가입과 다른 hard negative이며 4개 결정·
  158개 후보를 전부 검수했다.
- NH농협손해보험 `account.delete`는 독립 세션에서 서비스 안내 팝업을 닫고 전체 메뉴를
  열자 `로그인` 후보가 표시돼 현재 로그아웃 상태임을 확인했다. API가 `login_required`로
  자동 안전 종료했으며 연결·기록 오류가 아니다. 로그인이나 본인인증 없이
  `state_not_applicable`로 확정하고 2개 결정·82개 후보를 전부 검수했다.
- NH농협손해보험 `membership.join`은 홈의 `NH멤버스` 이벤트를 candidate_id로 실행해
  개인고객 로그인·지문/Face ID·`인증수단 신규 등록하기` 화면에 도달했다. 인증·개인정보
  등록 없이 `stop_for_user`로 종료했으며 3개 결정·92개 후보를 전부 검수했다. 로그인·
  생체인증 관련 5개 후보는 `unsafe`로 라벨링했다.
- NH농협손해보험 `membership.change`는 전체 메뉴와 `NH멤버스` 직접 진입을 별도 세션으로
  검증했다. 직접 진입은 로그인·생체인증 화면으로 이어져 현재 로그아웃 상태에서는 기존
  멤버십 변경이 적용되지 않는다. 전체 메뉴에 단순히 `로그인` 항목이 보인다는 이유로 API가
  `login_required`로 조기 종료한 오판은 별도 실패 사례로 보존했다. 4개 결정·161개 후보를
  전부 검수했고 인증·변경은 실행하지 않았다.
- NH농협손해보험 `membership.cancel`도 `NH멤버스` 진입 후 동일한 인증 경계를 확인했다.
  현재 로그아웃 상태에서는 기존 멤버십 해지가 적용되지 않는다. 전체 메뉴의 보험계약
  `해지/환급`은 멤버십 해지와 다른 의미적 hard negative다. 2개 결정·81개 후보를 전부
  검수했고 로그인·멤버십 해지·확정 실행은 0건이다.
- 커버리지 JSON에서 제주항공 `account.signup/delete`와 X `membership.cancel`이 Instagram
  행에 잘못 귀속된 문서 오류를 발견했다. Runtime·Review 원본의 `app_package`는 정상이었고,
  JSON 근거만 실제 앱 행으로 옮긴 뒤 Instagram 5셀을 미탐색으로 복원했다.
- 과거 TVING A/B는 검색 오류 진단 자료일 뿐 런타임 승자 선택에 사용하지 않는다.
- 공개 Navigation DB가 활성화된 B를 고정하고 현재 11개 앱의 절대 지표·고정 replay로
  수집 품질을 평가한다. validation·holdout 평가는 이후 새 미관측 앱으로만 수행한다.
