# ExitGuide API

FastAPI service for screenshot analysis.

Commands:

```powershell
..\..\scripts\Test-Api.ps1
..\..\scripts\Start-Api.ps1
```

The current `/v1/analyze` and `/v1/analyze/demo` endpoints return deterministic mock analysis for four synthetic scenarios while real OCR and LLM providers are being wired in.
