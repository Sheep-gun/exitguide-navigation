# ExitGuide Mobile

Android-first Expo app.

Commands:

```powershell
..\..\scripts\Typecheck-Mobile.ps1
..\..\scripts\Start-Mobile.ps1
```

The app can run phone-friendly demos, upload a selected screenshot to the API, render the mock analysis response, and revisit recent in-session results.
